linters:
  flake8:
    fixer: true
  rubocop:
    fixer: true
  phpcs:
    standard: PSR2
    fixer: true
  shellcheck:
    shell: bash
  csslint: {  }
files:
  ignore:
    - 'vendor/*'
    - 'bower_components/*'
    - 'node_modules/*'
fixers:
  enable: true

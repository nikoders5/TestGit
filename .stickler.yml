linters:
  flake8:
  
  rubocop:
  
  phpcs:
    standard: PSR2

  shellcheck:
    shell: bash
  csslint: {  }
  
  checkstyle:
        config: config/checkstyle_checks.xml
  
files:
  ignore:
    - 'vendor/*'
    - 'bower_components/*'
    - 'node_modules/*'
